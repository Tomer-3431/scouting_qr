name: scouting_qr
description: "A new Flutter project."
publish_to: 'none'
version: 2.1.0

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  qr_flutter: ^4.1.0
  orbit_standard_library: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
